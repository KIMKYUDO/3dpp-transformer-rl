문제: 경계를 벗어나는 위치를 선택하는 경우가 다반수 -> 학습 속도 지연

1. 보상스케일 현재 1e-3
수정 방식 -> 제거

이유: 무효를 피하기만 해도 Return ≈ 0


2. def _compute_valid_position_mask, _compute_valid_selection_mask, 
_compute_valid_orientation_mask

샘플링할 때 downsample을 한 값으로 계산되며 

실제 유효 위치 계산시 

해당값*patch+min_d > container_L로 계산

def _compute_valid_position_mask
가장 작은 박스의 길이를 놓을 시 경계를 넘는 위치 마스킹

def _compute_valid_selection_mask
선택한 위치에 넣을 시 경계를 넘는 박스(모든 방향 포함) 마스킹

def _compute_valid_orientation_mask
선택한 위치와 선택한 박스를 놓을 때 경계를 넘게되는 박스의 방향 마스킹

forward_decode 수정

3. 에피소드가 지날 때마다 박스의 수와 크기를 작은 상태에서 점점 크게하는 방식
utils.curriculum_scheduler.py
curriculum = CurriculumScheduler()

4. 박스 샘플링 단계에서 배치 내 모든 값이 -inf가 되어 분포를 생성하지 못해 error발생
-> 하나의 변만 확인 할 경우 _compute_valid_selection_mask에서 해당 박스를 못 넣는 경우 발생 ex) min_box = (15,48) pos = (55,55)

해결 방법:  _compute_valid_position_mask에서 하나의 변의 길이의 최소값이 아닌 두 개의 변의 최소값을 구하여 들어가는지 확인

5. 박스 방향 샘플링 단계에서 4번과 같은 문제 발생
-> 중복된 박스를 지우는 함수에서 오류 발생

6. 4번의 해결방법의 오류
-> 각 변의 최소를 뽑으면 두 변이 한 박스에서 추출한 것이 아닐 수 있음

해결 방법: 모든 회전 고려

7. 수정한 코드에 맞게 evaluate_actions함수도 수정

8. criticloss에서 inf가 발생하는 문제, ur향상에 도움이 되는 변경점
